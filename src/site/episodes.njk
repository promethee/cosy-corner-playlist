---
title: Tous les épisodes
layout: base.njk
permalink: /episodes/
bodyClass: page-episodes
---
<p class="count">{{ episodes.length }} épisodes</p>

<div class="episodes-grid">
  {% for episode in episodes %}
    <section class="episode-card">
      <h2 title={{ episode.pubDate | humanDate }}>
        {{ episode.title }}
      </h2>
      {% if episode.tracks.length > 0 %}
        <div class="track-grid">
          {% for track in episode.tracks %}
            {% if track.artist != "ALB" %}
              <a class="track-tile" href="https://duckduckgo.com/?q={{ (track.artist + ' ' + track.title) | urlencode }}&iar=videos&ia=videos" target="_blank" rel="noopener">
                <p class="track-artist">{{ track.artist }}</p>
                <p class="track-title">{{ track.title }}</p>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      {% else %}
        <p class="empty">Pas de playlist pour cet épisode.</p>
      {% endif %}
    </section>
  {% endfor %}
</div>